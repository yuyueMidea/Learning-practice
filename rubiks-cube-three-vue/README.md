用 **Three.js + Vue 3** 实现经典三阶魔方，能包含：

* 27 个独立小方块、六面正确配色
* 鼠标拖拽旋转整体视角
* 点击或拖动某一层，完成 `R / L / U / D / F / B` 转动动画
* 支持打乱、复原、步数记录、转动队列
* 可加入自动演示、计时器、还原公式播放
* 真实的 90° 平滑旋转，不是简单替换贴图

实现时核心是：每次转动把对应 9 个小方块临时挂到一个旋转组上，动画完成后再更新它们的坐标和朝向，这样逻辑和视觉都正确。
