# 微前端(Micro Frontend)实现详解

微前端是把"微服务"的理念搬到前端:将一个大型前端应用拆分成多个可以独立开发、独立部署、技术栈无关的小应用,再在运行时组合成一个完整的用户体验。下面从整体架构到具体落地方案,系统梳理一下。
<img width="1440" height="680" alt="image" src="https://github.com/user-attachments/assets/399dd3fe-eece-4464-b478-b9b0685ddfb7" />


这张图展示了微前端的典型结构:主应用(Host)负责路由分发和全局导航,把不同技术栈的子应用挂载到页面上,而底层由一套统一的运行时基座提供隔离和通信能力。下面展开讲讲具体怎么落地。

## 一、为什么要做微前端

当一个前端项目变得庞大(比如中后台系统聚合了十几个业务团队的功能)时会遇到:

- **技术栈锁死**:所有人被迫用同一版本的框架,老项目升级困难
- **构建耦合**:改一行代码,整个应用要重新构建、重新测试、重新发布
- **团队协作成本高**:多个团队在同一代码库里频繁冲突

微前端的核心思路就是:**把大应用拆成多个独立仓库、独立构建、独立部署的小应用,运行时再组合起来**,团队之间只需要约定好集成规范。

## 二、几种主流实现方式

### 1. iframe 方案
最简单粗暴,天然做到 JS 和样式的完全隔离。

- 优点:隔离彻底、实现成本几乎为零
- 缺点:路由与浏览器历史不同步、无法共享登录态和内存数据、弹窗/loading 蒙层难以跨 iframe 遮盖、白屏加载体验差
- 适用场景:内部系统对体验要求不高,或者接入完全不受控的第三方页面

### 2. 基于 Web Components 的方案
把每个子应用封装成一个自定义元素(Custom Element),利用浏览器原生的 Shadow DOM 做样式隔离。

```js
class MicroApp extends HTMLElement {
  connectedCallback() {
    this.attachShadow({ mode: 'open' });
    // 在 shadow root 里挂载子应用
    renderReactApp(this.shadowRoot);
  }
}
customElements.define('micro-app-a', MicroApp);
```

- 优点:标准化、样式隔离由浏览器原生保证
- 缺点:JS 全局变量依然会冲突,需要额外做沙箱

### 3. 基于路由分发的运行时集成(single-spa 系列)
这是目前最主流的方式。核心思想:主应用只负责根据路由决定"此刻该激活哪个子应用",子应用提供统一的生命周期钩子(`bootstrap`、`mount`、`unmount`)。

**single-spa** 是这套思路的鼻祖框架:

```js
import { registerApplication, start } from 'single-spa';

registerApplication({
  name: 'app-a',
  app: () => System.import('app-a'),
  activeWhen: '/app-a',
});

start();
```

**qiankun**(蚂蚁金服开源,国内使用最广)在 single-spa 基础上做了增强,加入了 JS 沙箱、样式隔离、预加载等开箱即用能力:

```js
import { registerMicroApps, start } from 'qiankun';

registerMicroApps([
  {
    name: 'app-a',
    entry: '//localhost:8080',
    container: '#subapp-container',
    activeRule: '/app-a',
  },
]);

start();
```

qiankun 内部会:
1. 用 `fetch` 拉取子应用的 HTML,解析出脚本内容(HTML Entry 方案)
2. 用 **快照沙箱** 或 **Proxy 沙箱** 隔离子应用对 `window` 的修改
3. 用动态添加 `data-qiankun` 属性等方式做 **样式隔离**(严格模式下用 Shadow DOM)

### 4. 打包时集成:Module Federation
Webpack 5 推出的 Module Federation(模块联邦)代表另一种思路——不在运行时用 HTML Entry 拉取整个应用,而是让应用之间可以在**构建产物层面**直接共享模块。

```js
// 子应用 webpack 配置:暴露模块
new ModuleFederationPlugin({
  name: 'app_a',
  filename: 'remoteEntry.js',
  exposes: { './Button': './src/Button' },
  shared: ['react', 'react-dom'],
});

// 主应用配置:消费远程模块
new ModuleFederationPlugin({
  remotes: { app_a: 'app_a@//localhost:8081/remoteEntry.js' },
  shared: ['react', 'react-dom'],
});
```

```js
// 主应用中直接像本地组件一样使用
const RemoteButton = React.lazy(() => import('app_a/Button'));
```

- 优点:共享依赖真正做到只加载一份,性能更好;不需要单独的沙箱运行时,心智负担小
- 缺点:更适合"组件级"共享,做整个页面级别的应用集成时,样式隔离、路由协调仍需自己处理;强依赖 Webpack 5(或 Vite 的相关插件、Rspack 等)

### 5. 基于 iframe 增强的方案(wujie 无界)
腾讯开源的 **wujie** 结合了 iframe 的天然隔离能力和 Web Components 的挂载方式:用 iframe 承载 JS 运行环境(天然沙箱、无需担心全局污染),但把 DOM 渲染到主应用页面的 Web Component 里(解决了 iframe 无法融入主应用样式/弹窗的问题)。这在体验和隔离性之间做了不错的折中。

## 三、绕不开的几个技术难点

无论选哪种方案,都要解决这四个问题:

**1. JS 沙箱**
子应用和主应用共享同一个 `window`,必须防止相互污染全局变量。常见实现:
- 快照沙箱:记录 mount 前的 window 状态,unmount 时还原(单实例,不支持多个子应用同时激活)
- Proxy 沙箱:用 `Proxy` 代理一个虚拟 window,子应用的所有全局读写都作用在这个代理对象上(支持多实例)

**2. 样式隔离**
- 简单粗暴:约定 CSS 前缀 / BEM 命名规范
- 工程化:CSS Modules、CSS-in-JS
- 运行时:动态给子应用所有样式规则加上唯一属性选择器前缀(qiankun 的做法),或直接用 Shadow DOM 硬隔离

**3. 应用间通信**
- 通过主应用注入的全局 EventBus / 发布订阅
- 通过 URL 参数传递少量状态
- 共享一个全局 Store(需要小心引入耦合,违背微前端"独立"的初衷)

**4. 公共依赖与资源加载**
避免每个子应用都各自打包一份 React、Vue 全家桶,可以通过 Module Federation 的 `shared` 配置,或者把公共库放到主应用的 external 里,子应用运行时复用。

## 四、方案怎么选

| 方案 | 隔离性 | 接入成本 | 适合场景 |
|---|---|---|---|
| iframe | 最强 | 极低 | 内部系统、可接受体验妥协 |
| qiankun / single-spa | 较强(需配置) | 中 | 多团队、多技术栈的中后台系统,国内生态成熟 |
| Module Federation | 弱(需自己处理) | 中高 | 同技术栈、组件级共享、追求极致性能 |
| wujie | 强 | 中 | 希望兼顾隔离性和用户体验的场景 |

对于国内团队,**qiankun** 目前依然是接入成本和生态最平衡的选择;如果子应用之间技术栈统一、更看重构建期的依赖共享,**Module Federation** 会是更轻量的路径。

