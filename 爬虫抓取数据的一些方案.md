# 公开网站合法爬虫方案分析

## 一、语言选型建议

先说结论：**Python 是爬虫首选**，理由如下：

| 维度 | Python | Java | Go |
|------|--------|------|-----|
| 生态成熟度 | ⭐⭐⭐⭐⭐ 最丰富 | ⭐⭐⭐ 较完善 | ⭐⭐⭐ 成长中 |
| 开发效率 | ⭐⭐⭐⭐⭐ 极高 | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐ 较高 |
| 并发性能 | ⭐⭐⭐ asyncio可用 | ⭐⭐⭐⭐ 线程池强 | ⭐⭐⭐⭐⭐ goroutine天然优势 |
| 反爬处理 | ⭐⭐⭐⭐⭐ 工具最多 | ⭐⭐⭐ 一般 | ⭐⭐⭐ 较少 |
| 适合场景 | 全场景 | 企业级大规模 | 高并发简单页面 |

> **结论**：个人/小团队 → Python；高并发轻量任务 → Go；已有Java技术栈 → Java

---

## 二、技术选型细节（以Python为主）

### 场景分类选型

```
目标网站
   │
   ├─ 静态HTML（直接返回内容）
   │      └─ requests + BeautifulSoup / lxml
   │
   ├─ 动态渲染（JS渲染、SPA、React/Vue）
   │      ├─ 轻量 → httpx + playwright（推荐）
   │      └─ 重量 → Selenium（备用，慢）
   │
   ├─ 有 robots.txt / 频率限制
   │      └─ Scrapy（内置中间件、限速、重试）
   │
   └─ 大规模 / 分布式
          └─ Scrapy + Redis（scrapy-redis）
```

### 核心库速查

| 用途 | 推荐库 | 说明 |
|------|--------|------|
| HTTP请求 | `httpx` | 支持async，比requests更现代 |
| HTML解析 | `BeautifulSoup4` + `lxml` | bs4写法简单，lxml速度快 |
| CSS/XPath选择 | `parsel` | Scrapy内置，独立也可用 |
| JS渲染 | `playwright` | 微软出品，比selenium快 |
| 框架级 | `Scrapy` | 大项目首选，功能完整 |
| 反爬处理 | `fake-useragent`、`httpx` | UA轮换、代理支持 |

---

## 三、分场景实现方案

### 方案A：静态页面（最轻量）

适合：新闻、博客、文档类网站

```python
# 技术栈：httpx + BeautifulSoup + json存储
import httpx
from bs4 import BeautifulSoup
import json, time, random
from pathlib import Path

HEADERS = {
    "User-Agent": "Mozilla/5.0 (compatible; MyResearchBot/1.0)"
}

def fetch_page(url: str) -> BeautifulSoup | None:
    try:
        resp = httpx.get(url, headers=HEADERS, timeout=10, follow_redirects=True)
        resp.raise_for_status()
        return BeautifulSoup(resp.text, "lxml")
    except Exception as e:
        print(f"[ERROR] {url} -> {e}")
        return None

def parse_article(soup: BeautifulSoup, url: str) -> dict:
    return {
        "url": url,
        "title": soup.find("h1").get_text(strip=True) if soup.find("h1") else "",
        "content": soup.find("article").get_text(strip=True) if soup.find("article") else "",
        "crawled_at": time.strftime("%Y-%m-%d %H:%M:%S"),
    }

def save_json(data: list[dict], path: str = "output.json"):
    Path(path).write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    print(f"[SAVED] {len(data)} items -> {path}")

# 主流程
urls = ["https://example.com/article/1", "https://example.com/article/2"]
results = []

for url in urls:
    soup = fetch_page(url)
    if soup:
        results.append(parse_article(soup, url))
    time.sleep(random.uniform(1.5, 3.0))  # 礼貌性延迟，避免给服务器造成压力

save_json(results)
```

---

### 方案B：动态页面（JS渲染）

适合：React/Vue SPA、需要登录后操作、懒加载内容

```python
# 技术栈：playwright (async)
import asyncio
from playwright.async_api import async_playwright
import json, time

async def scrape_dynamic(url: str) -> dict:
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        page = await browser.new_page()

        # 拦截非必要资源，提升速度
        await page.route("**/*.{png,jpg,gif,woff2}", lambda r: r.abort())

        await page.goto(url, wait_until="networkidle", timeout=30000)

        # 等待关键元素出现
        await page.wait_for_selector(".content", timeout=10000)

        # 提取内容
        title = await page.text_content("h1")
        content = await page.text_content(".content")

        await browser.close()
        return {
            "url": url,
            "title": title.strip() if title else "",
            "content": content.strip() if content else "",
            "crawled_at": time.strftime("%Y-%m-%d %H:%M:%S"),
        }

async def main():
    urls = ["https://example.com/dynamic-page"]
    results = [await scrape_dynamic(url) for url in urls]
    with open("dynamic_output.json", "w", encoding="utf-8") as f:
        json.dump(results, f, ensure_ascii=False, indent=2)

asyncio.run(main())
```

---

### 方案C：Scrapy 框架（中大型项目）

适合：多页面、多层级、需要断点续爬的场景

```python
# scrapy startproject myspider && cd myspider
# 文件：myspider/spiders/news_spider.py

import scrapy
import json
from pathlib import Path

class NewsSpider(scrapy.Spider):
    name = "news"
    allowed_domains = ["example-news.com"]
    start_urls = ["https://example-news.com/latest"]

    custom_settings = {
        "DOWNLOAD_DELAY": 2,           # 礼貌延迟
        "CONCURRENT_REQUESTS": 4,      # 并发数
        "ROBOTSTXT_OBEY": True,        # 遵守robots.txt（合法抓取必备）
        "DEFAULT_REQUEST_HEADERS": {
            "User-Agent": "MyBot/1.0 (research purpose)"
        },
    }

    def parse(self, response):
        # 提取列表页文章链接
        for link in response.css("a.article-link::attr(href)").getall():
            yield response.follow(link, callback=self.parse_article)

        # 翻页
        next_page = response.css("a.next-page::attr(href)").get()
        if next_page:
            yield response.follow(next_page, callback=self.parse)

    def parse_article(self, response):
        yield {
            "url": response.url,
            "title": response.css("h1::text").get("").strip(),
            "content": " ".join(response.css("article p::text").getall()),
            "published": response.css("time::attr(datetime)").get(""),
        }

# 运行命令：
# scrapy crawl news -o output.jsonl  （自动输出到 JSONL 文件）
```

---

## 四、本地存储方案对比

| 格式 | 适用场景 | 优点 | 缺点 |
|------|----------|------|------|
| **JSON** | 层级结构数据、字段不固定 | 直观、Python原生支持 | 大文件读写慢 |
| **JSONL** | 流式写入、大量条目 | 逐行追加，不怕中断 | 不如JSON易读 |
| **CSV** | 表格型数据、Excel查看 | 通用、Excel直接打开 | 不支持嵌套结构 |
| **SQLite3** | 需要查询/去重/统计 | SQL查询、防重复 | 稍复杂 |

### 推荐：JSONL（流式写入，最适合爬虫）

```python
# 核心存储工具类 —— 支持断点续爬
import json, sqlite3
from pathlib import Path

class Storage:
    """统一存储接口，支持 JSONL / CSV / SQLite"""

    # ── JSONL（推荐：边爬边写，不怕崩溃）──
    @staticmethod
    def append_jsonl(record: dict, path: str = "data.jsonl"):
        with open(path, "a", encoding="utf-8") as f:
            f.write(json.dumps(record, ensure_ascii=False) + "\n")

    # ── SQLite（需要去重/查询时使用）──
    @staticmethod
    def init_sqlite(db: str = "data.db") -> sqlite3.Connection:
        conn = sqlite3.connect(db)
        conn.execute("""
            CREATE TABLE IF NOT EXISTS pages (
                id      INTEGER PRIMARY KEY AUTOINCREMENT,
                url     TEXT UNIQUE,          -- UNIQUE 自动去重
                title   TEXT,
                content TEXT,
                crawled_at TEXT
            )
        """)
        conn.commit()
        return conn

    @staticmethod
    def insert_sqlite(conn: sqlite3.Connection, record: dict):
        try:
            conn.execute(
                "INSERT OR IGNORE INTO pages (url,title,content,crawled_at) VALUES (?,?,?,?)",
                (record["url"], record["title"], record["content"], record["crawled_at"])
            )
            conn.commit()
        except Exception as e:
            print(f"[DB ERROR] {e}")
```

---

## 五、合法抓取检查清单

在开始爬取前，务必确认以下几点：

```
✅ 查看 robots.txt        https://目标站/robots.txt
✅ 设置合理爬取间隔       >= 1~2秒，避免给服务器造成压力
✅ 标明 User-Agent        说明用途，如 "MyBot/1.0 (research)"
✅ 只抓公开内容           不绕过登录墙、不抓付费内容
✅ 遵守网站服务条款       ToS中明确禁止爬取的须放弃
✅ 数据仅用于合法用途     不用于商业竞争、不侵犯版权
```

---

## 六、推荐组合方案汇总

```
【个人研究 / 小项目】
  Python + httpx + BeautifulSoup → 存 JSONL
  （静态页加 playwright 处理动态页）

【中型项目 / 多站点】
  Python + Scrapy → 输出 .jsonl / SQLite
  （内置调度、重试、限速，省心）

【高并发 / 简单结构】
  Go + colly 框架 → 存 JSON / SQLite
  （goroutine 天然并发，性能出色）
```
